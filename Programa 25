//[DESAFIO] Crie um programa que leia o tamanho de três segmentos de reta. Analise seus comprimentos e diga se é possível formar um triângulo com essas
//retas. Matematicamente, para três segmentos formarem um triângulo, o comprimento de cada lado deve ser menor que a soma dos outros dois.
//Autor: Vitória Kamilly Lago Soares

programa 25{
	funcao inicio() {
		inteiro a, b, c
		
		escreva("Informe o comprimento do primeiro segmento: ")
		leia(a)
		escreva("Informe o comprimento do segundo segmento: ")
		leia(b)
		escreva("Informe o comprimento do terceiro segmento: ")
		leia(c)
		
		se(a<b+c e b<a+c e c<a+b){
		    escreva("Os segmentos formam um triângulo.")
		}senao{
		    escreva("Os segmentos não formam um triângulo.")
		}
	}
}
